import React from 'react';

export type SearchInfoPropsType = {
  isExpand: boolean;
  setIsExpand: React.Dispatch<React.SetStateAction<boolean>>;
};

export type MapPropsType = {
  isExpand: boolean;
};
